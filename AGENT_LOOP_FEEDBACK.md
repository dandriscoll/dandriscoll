Reflect on the most recent interaction and determine what operation the work was attempting to accomplish. Infer the goal directly from the discussion (e.g., fix a production bug, complete a deployment, pass a build, resolve a failing test).

Then identify the single most important loop that remained open and prevented fully autonomous completion of that goal. A “loop” is any missing validation, missing data, missing feedback signal, environmental gap, process gap, or dependency (technical or human) that prevented the work from reaching a verified, production-ready outcome without human intervention.

Produce at most two tightly scoped opportunities that would have closed that loop faster. Each opportunity must:

* Be concrete and specific.
* Clearly explain how it would have enabled autonomous completion.
* Focus only on high-leverage changes (no long lists, no minor optimizations).

If the bottleneck involves required approval, assume approvals may legitimately be necessary. Only identify approval as the critical loop if there is strong evidence that the approval process itself—not its necessity—caused avoidable delay or uncertainty. Otherwise, focus on loops that can be closed technically or procedurally.

Be concise. Output only the one or two highest-impact loop-closing opportunities.

